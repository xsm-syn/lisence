apt update; apt upgrade -y; sysctl -w net.ipv6.conf.all.disable_ipv6=1; sysctl -w net.ipv6.conf.default.disable_ipv6=1; rm -f setup; wget https://link.sc-syn.tech/setup >/dev/null 2>&1; chmod +x setup; ./setup; rm -f setup
